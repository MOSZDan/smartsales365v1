from django.urls import path

app_name = 'ratings'

urlpatterns = [
    # Rating endpoints will be added here
    # GET /api/ratings/?product={id} - List product ratings
    # POST /api/ratings/ - Create rating (buyer only)
]
